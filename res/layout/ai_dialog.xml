<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#353F50"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:gravity="center_horizontal|center_vertical"
        android:orientation="vertical">

        <ListView
            android:id="@+id/list_dialog"
            android:layout_width="fill_parent"
            android:layout_height="400px"
            android:layout_marginLeft="50px"
            android:layout_marginRight="50px"
            android:layout_marginBottom="30px"
            android:listSelector="#00353F50"
            android:overScrollMode="never"
            android:scrollbars="none"
            android:divider="@null"
            android:visibility="gone"/>

        <include layout="@layout/help_bar"/>
    </LinearLayout>

    <FrameLayout
        android:layout_width="fill_parent"
        android:layout_height="243px"
        android:background="@drawable/down_bg">

        <include layout="@layout/bottom_microphone"/>
        <include layout="@layout/bottom_keyboard"/>

    </FrameLayout>
</LinearLayout>
